// ============================================================
// services/rag/constants.js
// Every tunable default in one place — change once, applies
// everywhere it's used.
// ============================================================

export const DEFAULT_COLLECTION = "rag_default";

export const DEFAULT_CHUNK_SIZE = 1200;
export const DEFAULT_CHUNK_OVERLAP = 180;

export const DEFAULT_VECTOR_DIMENSIONS = 384;

export const DEFAULT_PAGE_BATCH_SIZE = 6;
export const DEFAULT_BATCH_CONCURRENCY = 1;